# KakaPin · 双击启动

这是「本机服务 + 浏览器」的双击启动版，不是已经打包好的 `.app` / `.exe` 安装包。照片、项目、渲染都在本机；正常启动不需要 Codex。

## 以后如何打开

- **Mac**：在 Finder 中双击 `启动 KakaPin.command`。
- **Windows**：在资源管理器中双击 `启动 KakaPin.bat`。

检查通过后自动打开 `http://127.0.0.1:4177/`。使用期间保留启动窗口；结束时在该窗口按 `Ctrl+C`。导出期间不要退出。仅关闭浏览器不会停止服务，再次双击可重新打开。

重复双击不会启动两个相同服务。若端口是其他程序或另一份项目占用，启动器会说明原因，不会强行关闭程序。

## 换一台电脑：首次准备

1. 安装 [Node.js LTS](https://nodejs.org/en/download)，支持 22 或更新的 LTS，当前 Mac 已用 24 验证。安装后重新打开启动窗口。
2. 准备 [FFmpeg](https://ffmpeg.org/download.html) 的对应系统版本，需同时有 `ffmpeg` 和 `ffprobe`，包含 `libx264` 与 `xfade`。将文件放进项目 `bin/`，或把它们所在目录加入 PATH。Mac 也自动查找 Homebrew 的常用安装位置。
3. 如果要使用画面小字，把**有使用权的默陌专辑字体**放进 `fonts/`，命名为 `momo.ttf` 或 `momo.otf`。本项目不擅自分发字体。原 Mac 已安装的字体自动识别。
4. 双击启动。首次缺少项目依赖时，输入 `y` 同意联网安装；下载的是本项目的 Node 依赖，不会安装系统软件。依赖安装成功后，后续直接双击。

照片和视频不会因安装依赖被上传。无文字影片不要求默陌字体；缺少字体又有小字时，导出会提醒，不会悄悄更换字体。

## Mac 与 Windows 之间迁移

停止渲染和服务后，复制整个项目文件夹，但**不要复制 `node_modules`**（包含系统相关的 Sharp 组件），也不需要复制 `.backups` 和测试产物。在新系统首次启动时重新安装依赖。

要继续旧项目，必须带上隐藏目录 `.runtime`，其中包含项目 JSON、素材和输出。Mac Finder 可按 `⌘⇧.` 显示隐藏文件。不要只复制网页或只复制启动器。迁移前建议保留原目录作为备份。

字体和 FFmpeg 需分别准备对应系统可用的文件。Mac 与 Windows 不共享同一套二进制。离线电脑应先在同系统、同架构的联网电脑准备依赖，再一并迁移。

两台电脑可以读写同样的项目结构，但**没有云同步或跨电脑协同**。不要让两台电脑同时写同一个网盘里的运行目录；关闭服务后再复制。

## 自定义位置或端口

先在已打开的启动窗口按 `Ctrl+C` 停止服务，再复制 `kakapin.config.example.json` 为 `kakapin.config.json`，修改需要的字段。相对路径以项目目录为起点，不受终端当前目录影响。空的工具路径表示自动查找。不要让同一运行目录同时运行在两个端口。

```json
{
  "port": 4177,
  "runtimeDir": ".runtime",
  "ffmpeg": "bin/ffmpeg.exe",
  "ffprobe": "bin/ffprobe.exe",
  "font": "fonts/momo.ttf"
}
```

以上二进制名用于 Windows；Mac 去掉 `.exe`。Windows 的绝对路径可使用 `D:/Tools/ffmpeg/bin/ffmpeg.exe`，避免 JSON 反斜杠转义错误。更改运行目录后看到空项目库是正常的，旧项目仍留在原目录，不会自动搬迁。

也支持环境变量 `PORT`、`ALBUM_RUNTIME_DIR`、`KAKAPIN_FFMPEG`、`KAKAPIN_FFPROBE`、`KAKAPIN_FONT`，优先于配置文件。

## 常见问题

- 窗口显示缺少 Node.js / FFmpeg：先完成上述首次准备，不需要反复点击启动。
- Mac 提示没有执行权限：在项目目录运行 `chmod +x "启动 KakaPin.command"`。如果系统显示安全警告，应先确认文件来源，不需要关闭系统安全功能。
- `EADDRINUSE` / 端口被占用：若已启动，直接访问现有页面；若被其他应用占用，可修改 `port`（如 4180），不需要杀掉其他程序。
- Sharp / 依赖无法加载：通常是复制了另一个系统的 `node_modules`。在当前系统的项目目录运行 `npm ci` 重新安装。
- 某个 HEIC / 视频格式无法导入：编解码支持取决于当前系统的 Sharp / FFmpeg 构建；可先转为 JPG / H.264 MP4。不能保证每个平台的第三方解码器完全相同。
- 只检查环境、不启动：在项目目录运行 `npm run doctor`。
- 当前程序仅监听 `127.0.0.1`，不向局域网或公网开放。

## 新模板在哪里

打开项目 → 左侧 **模板 → 珍藏系列 · 新**：暗房印样、手工剪贴、杂志跨页、实体影集、网格画廊、水墨显影。

展开「模板用法与动效」可以应用推荐转场、调整照片分组；网格画廊可关闭逐张展开。改变分组需查看影响并确认，单独设置的页面默认保留，可撤销。四图模式下若全部为竖图，会自动排成四列，避免把竖图裁成横图。

新增模板不添加固定文案，不旋转照片。视频不套用照片停留时长。全部模板支持自定义背景色；影集和杂志的纸页属于相册材质，不随外部底色变黑。

## 验证范围

Mac 上验证了启动脚本、环境检查、独立服务启动、重复启动识别，以及六款模板实际 MP4 导出。Windows 已适配启动脚本、可执行文件查找、含空格/中文路径、字体路径与滤镜脚本文件；**尚未在真实 Windows 机器上运行验收**。
